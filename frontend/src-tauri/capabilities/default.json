{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only the main desktop window may invoke Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
