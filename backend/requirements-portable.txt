# Pinned runtime dependencies for the embedded Windows backend (PyInstaller onefile).
# Local API and persistence
fastapi==0.139.0
uvicorn==0.51.0
SQLAlchemy==2.0.51
httpx==0.28.1

# Local QR image generation. Keep Pillow pinned because it includes native modules.
qrcode[pil]==8.2
Pillow==12.3.0

# Local NLP
jieba==0.42.1
snownlp==0.12.3

# Single-file packaging
PyInstaller==6.21.0
