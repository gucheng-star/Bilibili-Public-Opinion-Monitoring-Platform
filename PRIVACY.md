# 隐私政策

**更新日期：2026 年 9 月 4 日**

本隐私政策适用于“B站舆论监测平台”（下称“本应用”）的 Windows 桌面版。请在使用前阅读本政策。

本应用采用本地优先的设计：评论抓取、Cookie、数据库和大部分分析均在你的电脑上完成。项目不运营用于接收这些数据的自建服务端。

项目维护者：gucheng。隐私与安全问题反馈渠道：[GitHub Issues](https://github.com/gucheng-star/Bilibili-Public-Opinion-Monitoring-Platform/issues)。

## 1. 我们处理哪些数据

为实现你主动使用的功能，本应用会在本机处理以下数据：

| 数据类别 | 示例 | 用途与存储位置 |
| --- | --- | --- |
| B 站登录凭据 | 扫码登录后取得的 Cookie | 代表你向 B 站请求评论数据；加密后保存在 EXE 同级 `data/` 目录。 |
| B 站公开或可访问的视频与评论数据 | BV 号、视频标题、评论文本、发布时间、公开展示的地域/性别等字段 | 抓取、筛选、统计、生成图表、词云和本机历史记录；保存在本机 SQLite 数据库。 |
| 你的本地配置 | 抓取数量、请求间隔、主题、模型服务地址、模型名称和 API Key | 保存使用偏好及可选模型能力；配置保存在本机。Cookie 与 API Key 在 Windows 桌面版使用当前 Windows 用户的 DPAPI 加密。 |
| 本地运行信息 | 本地日志、任务状态、版本更新缓存 | 排查本机运行问题、显示任务进度和完成更新；保存在 EXE 同级 `data/` 目录。 |

本应用不要求你填写姓名、手机号、身份证号等信息。请不要在视频链接、模型配置备注或其他可输入位置主动写入不必要的个人信息。

## 2. 数据如何流转

### 2.1 访问 B 站

当你扫码登录、获取视频信息或开始抓取时，本应用会从你的电脑直接请求 B 站相关服务。请求使用你的网络出口 IP 和本机 Cookie，以完成你发起的操作。项目不通过自建服务器转发这些请求。

因此，B 站会按照其自身规则处理相关网络请求和登录状态；请同时阅读并遵守 B 站的服务条款与隐私规则。

### 2.2 本地分析

默认情感分析、筛选、图表、关键词与词云在本机完成。改变筛选条件、切换图表或重新计算词云，不会自动调用大模型服务。

### 2.3 你主动启用大模型时

情绪与表达风格分析、AI 舆情简报是可选功能，只有在你完成配置并主动触发后才会请求你选择的模型服务商。为完成该次请求，本应用会发送经过规模限制的必要评论文本和分析指令；不会发送 Cookie、模型 API Key、用户名或评论 ID。

其中，AI 舆情简报最多使用 40 条、合计不超过 12,000 字的代表性评论样本；情绪与表达风格分析会按任务需要处理待分析评论。评论文本本身仍可能含有个人信息，请在启用前自行判断内容和服务商是否适合。

模型服务商由你自行选择和配置，其数据处理受该服务商的规则约束。使用前请确认服务商、接口地址、所在地区和其隐私政策符合你的要求；不要将敏感或不应出境的内容提交给不适合的服务。

删除本机数据或退出登录不能撤回已发送给 B 站、模型服务商或 GitHub 的网络请求。对于已发送给模型服务商的内容，请向相应服务商了解其保留、训练使用与删除规则。

### 2.4 下载与更新

当你访问 GitHub Releases 或使用应用的更新功能时，GitHub 等相应服务提供商可能依据其规则处理你的网络请求信息（例如 IP 地址、浏览器或客户端信息）。这些信息不由本应用的项目方收集或控制。

## 3. 存储、安全与保留期限

- 可写数据默认位于 EXE 同级的 `data/` 目录，不会主动写入 Windows 用户目录。
- Cookie 与 API Key 在桌面版中使用 Windows DPAPI 加密，通常只能由保存它们的 Windows 用户在同一台电脑上解密。
- DPAPI 只用于 Cookie 与 API Key，不会加密评论与历史 SQLite 数据库本身。复制、备份或共享整个 `data/` 目录前，请确认其中的评论与历史记录已得到妥善保护。
- 本应用不主动将本地数据库、Cookie、模型配置或分析历史上传至项目方服务器。
- 数据由你控制并保留，直至你删除对应历史、退出登录、清空配置或删除 `data/` 目录。删除 `data/` 不可恢复，请先备份。
- 将 `data/` 复制到另一台电脑或切换 Windows 用户后，历史数据库可能仍可读取，但 Cookie 与 API Key 通常需要重新录入。

任何本地软件都无法替代设备安全措施。请使用受密码保护的 Windows 账户，避免把包含 `data/` 的文件夹共享给他人，并谨慎备份。

## 4. 你的选择与控制

你可以随时：

- 不登录 B 站，或在应用中退出登录；退出登录会清除本机当前登录凭据，不会删除已有分析记录和模型配置。
- 不配置或不使用大模型功能；默认本地分析仍可使用。
- 删除不再需要的分析记录或舆情事件；删除事件不会删除其来源的视频分析记录。
- 退出应用后，备份、移动或删除 EXE 同级 `data/` 目录；删除前请确认其中的数据不再需要，避免文件仍被程序占用。
- 不使用更新功能，或仅从本仓库 GitHub Releases 手动下载更新。

本应用不集中保存你的本地数据，因此项目方通常无法代你检索、更正、导出或删除设备中的数据；请直接在你的电脑上完成上述操作。

## 5. 未成年人与公开评论数据

本应用面向能够独立理解并遵守适用法律、平台规则的使用者。请勿以本应用处理未成年人不应处理的数据，也不要将评论数据用于违法、歧视、骚扰、画像推断或其他不当用途。

即使评论内容来自公开页面，也可能涉及个人信息。请遵循数据最小化原则，仅为合理、必要的分析目的处理，避免不当传播、再识别或公开展示评论者信息。

## 6. 政策更新与联系

功能或数据处理方式发生实质变化时，本政策会随版本更新。最新版本以仓库根目录的本文件为准；“更新日期”会反映最近一次修订时间。

如发现隐私或安全问题，请通过本仓库的 [Issues](https://github.com/gucheng-star/Bilibili-Public-Opinion-Monitoring-Platform/issues) 反馈。请勿在公开反馈中提交 Cookie、API Key、完整日志、二维码内容或未脱敏的评论数据。

## 7. 使用本应用即表示

你理解本应用会在你的设备上按本政策处理数据；当你主动登录 B 站、使用可选模型服务或下载更新时，相应第三方服务将按照其自身规则处理必要的网络请求。你应遵守适用法律法规、B站服务条款和接口限制，并对自己的使用行为负责。
